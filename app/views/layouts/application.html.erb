<!DOCTYPE html>
<html>
<head>
  <title>Workspace</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="container-flex">
  <div class="clearfix header">
    <% if user_signed_in? %>
      <h1 class="pull-right"><%= link_to current_user.username, user_path(current_user) %></h1>
      <h2 class="pull-left"><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></h2>
    <% end %>
  </div>

  <%= yield %>

</body>
</html>
